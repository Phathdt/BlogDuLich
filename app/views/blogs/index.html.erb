

<% @categories.each do |category| %>
<section id="packages" class="inverse">
	<div class="container">
		<div class="row">
			<div class="col-xs-12">
				<div class="section-title text-center">
					<h1><%= category.name %></h1>
				</div>
			</div>
		</div>
		
		<div class="row">
		<% @blogs.each do |blog| %>
		<% if blog.blogcategory_id == category.id %>
		<%= link_to blog_path(blog), :action => 'go' do %>
			<div class="col-xs-6 col-sm-6 col-md-4">
				<div class="package-list wow fadeInUp">
					
						<div class="package-thumb">
							<%= image_tag blog.image.url, width:'100%' %>
						</div>
						<div class="package-info">
							<h3><%= blog.title %></h3>
							<span class="pull-left">
								<i class="fa fa-star"></i>
								<i class="fa fa-star"></i>
								<i class="fa fa-star"></i>
								<i class="fa fa-star"></i>
								<i class="fa fa-star"></i>
								<span class="review-count pull-right">- 100 review</span>
							</span>
							
						</div>
					</a>
				</div>
			</div>
			<% end %>
		<% end %>
		
		<% end %>
		</div>
		
			
			
	</div>
</section>
<!-- end Packages -->
<% end %>