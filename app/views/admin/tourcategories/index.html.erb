<h2 class="page-header">Manage Tour Categories <%= link_to "Create New", new_admin_tourcategory_path, class:'btn btn-success pull-right' %></h2>
<table class="table table-striped">
<tr>
  <th>Tour Category Title</th>
  <th>Create Date</th>
  <th>Actions</th>
</tr>
<% @categories.each do |category| %>
	<tr>
	  <td><%= category.name %></td>  
	  <td><%= category.created_at.to_time.strftime('%B %e at %l:%M %p') %></td>    
	  <td><%= link_to "Edit", edit_admin_tourcategory_path(category), class:'btn btn-primary' %> <%= link_to "Delete", admin_tourcategory_path(category), class:'btn btn-danger', method: :delete, data: {confirm: 'Are you sure'} %></td>
	</tr>
<% end %>
</table>
